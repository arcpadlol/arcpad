{
  "name": "citizen-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Citizen bonding-curve launchpad contracts for Arc testnet",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "deploy:arc": "hardhat run scripts/deploy.js --network arcTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.26.0"
  }
}
